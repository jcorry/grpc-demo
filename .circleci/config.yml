version: 2.1
jobs:
  protoc:
    docker:
      - image: circleci/golang
    steps:
      - run: protoc --version
orbs:
  protobuf: izumin5210/protobuf@0.1.0
workflows:
  dep:
    jobs:
      - protobuf/install
      - protoc
